# Public URL of the Convex deployment the app talks to (see packages/backend/.env.local).
EXPO_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
# Optional. Must match APP_CLIENT_KEY in the Convex deployment env when that is set.
# EXPO_PUBLIC_CLIENT_KEY=
